# --- Secrets: never commit ---
creds.txt
*.env
.env

# --- Model weights & big artifacts (keep these out of git; sync separately) ---
*.gguf
*.safetensors
*.npz
*.bin
*.pt
*.pth
*.tar.gz
*.tgz
*.7z
*.zip
# macOS hidden split/checksum copies of weights
.model.safetensors*

# --- Native binaries ---
*.dll
*.exe
*.so
*.dylib
# downloaded llama.cpp build (binaries + CUDA runtime + .variant marker)
bin/llama.cpp/

# --- Runtime / scratch ---
tmp/
logs/
screenshots/
__pycache__/
*.pyc
.claude/

# --- OS / editor cruft ---
.DS_Store
._*
Thumbs.db

# partial streamed downloads
*.part
# per-machine weight checksum sidecars (cache for the staleness check)
*.sha256
